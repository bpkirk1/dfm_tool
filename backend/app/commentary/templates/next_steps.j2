{% if has_steps %}
In priority order, resolve critical corrections first, then confirm the supplier-capability items, then close out the manual checks (listed below).
{% else %}
No blocking actions were identified for this run; proceed to standard DFM review.
{% endif %}

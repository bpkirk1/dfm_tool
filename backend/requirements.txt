# DFM tool backend — local-first, no heavy CAD kernel in Phase 1.
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
pydantic>=2.7
PyYAML>=6.0
pdfplumber>=0.11
pytest>=8.3
httpx>=0.27
reportlab>=4.2
# Optional: DXF export of the developed flat pattern for supplier packets.
# Lazy-imported — the app degrades gracefully if it is not installed.
ezdxf>=1.1
